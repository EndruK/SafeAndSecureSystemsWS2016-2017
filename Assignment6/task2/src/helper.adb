-- André Karge 110033
-- K. Gerrit Lünsdorf 100141

-- pragma SPARK_Mode(on);
package body Helper is
    procedure Swap(A: in out Natural_Array) is
    begin -- Swap
        A := A;
    end Swap;
end Helper;
